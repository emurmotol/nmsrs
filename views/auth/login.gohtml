{{define "content"}}
<div id="error"></div>
<form>
    <div class="form-group">
        <input type="text" class="form-control" name="username" id="username" placeholder="Username">
    </div>
    <div class="form-group">
        <input type="password" class="form-control" name="password" id="password" placeholder="Password">
    </div>
    <button type="button" id="form-login-submit" class="btn btn-outline-primary btn-block">Login</button>
</form>
{{end}} {{define "script"}}
<script>
    $(function () {
        var fields = [
            "username",
            "password"
        ];

        $("#form-login-submit").on("click", function () {
            validateForm("/login", fields, {
                "username": $("#" + fields[0]).val(),
                "password": $("#" + fields[1]).val()
            });
        });
    });

</script>
{{end}}